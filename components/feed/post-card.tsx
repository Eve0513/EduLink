import { MessageCircle, Repeat2, ThumbsUp } from "lucide-react";
import { formatTimeAgo } from "@/lib/utils";

export function PostCard({ post, author }: { post: { id: string; content: string; image_url: string | null; created_at: string; creator_id: string }; author?: { full_name: string; headline: string | null; avatar_url: string | null; role: string } }) {
  const name = author?.full_name ?? "Membru EduLink";
  return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="flex items-start gap-3 p-5"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-full bg-[#e5f4f6] font-bold text-[#0e5e6f]">{name.slice(0, 1)}</div><div><div className="flex flex-wrap items-center gap-2"><h2 className="font-bold text-slate-900">{name}</h2><span className="rounded-full bg-[#e5f4f6] px-2 py-0.5 text-[11px] font-bold text-[#0e5e6f]">{author?.role === "company" ? "Companie" : author?.role === "institution" ? "Instituție" : "Student"}</span></div><p className="text-xs text-slate-500">{author?.headline ?? "Comunitatea EduLink"} · {formatTimeAgo(post.created_at)}</p></div></div><p className="whitespace-pre-wrap px-5 pb-5 leading-7 text-slate-700">{post.content}</p>{post.image_url && <img src={post.image_url} alt="Imaginea postării" className="max-h-[34rem] w-full object-cover" />}<div className="grid grid-cols-3 border-t border-slate-100 px-3 py-2"><Action icon={<ThumbsUp />} label="Apreciază" /><Action icon={<MessageCircle />} label="Comentează" /><Action icon={<Repeat2 />} label="Distribuie" /></div></article>;
}
function Action({ icon, label }: { icon: React.ReactNode; label: string }) { return <button type="button" className="flex items-center justify-center gap-2 rounded-lg py-2 text-sm font-semibold text-slate-600 hover:bg-slate-50">{icon}{label}</button>; }
