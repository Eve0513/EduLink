"use client";

import { ExternalLink, Loader2, Sparkles, X } from "lucide-react";
import { useEffect, useState } from "react";
import { useSearchParams } from "next/navigation";
import { toast } from "sonner";
import { StudentHeader } from "@/components/dashboard/student-header";

export function AIHubClient({ portfolioSlug, name, avatarUrl }: { portfolioSlug: string | null; name: string; avatarUrl: string | null }) {
  const params = useSearchParams(); const [generating, setGenerating] = useState(false); const [atsScore, setAtsScore] = useState<number | null>(null); const [intent, setIntent] = useState<"cv" | "portfolio" | null>(null);
  useEffect(() => { const value = params.get("intent"); if (value === "cv" || value === "portfolio") setIntent(value); }, [params]);
  async function handleGenerateCV() { setIntent(null); setGenerating(true); try { const res = await fetch("/api/ai/generate-cv", { method: "POST" }); const data: { ats_score?: number } = await res.json(); if (!res.ok) { toast.error("CV-ul nu a putut fi generat acum. Verifică profilul și încearcă din nou."); return; } setAtsScore(data.ats_score ?? null); toast.success(`CV generat. Scor ATS: ${data.ats_score ?? "N/A"}%`); } catch { toast.error("Eroare de rețea. Încearcă din nou."); } finally { setGenerating(false); } }
  function continueIntent() { if (intent === "cv") { void handleGenerateCV(); return; } setIntent(null); if (!portfolioSlug) { toast.info("Completează profilul pentru a primi adresa portofoliului."); return; } window.open(`/portofoliu/${portfolioSlug}`, "_blank", "noopener,noreferrer"); }
  return <div className="min-h-screen bg-[#f5f8f9]"><StudentHeader name={name} avatarUrl={avatarUrl} current="ai" /><main className="mx-auto max-w-4xl space-y-8 px-5 py-8 sm:px-6"><div className="border-b border-slate-200 pb-6"><p className="text-xs font-bold uppercase tracking-wider text-[#0e5e6f]">Spațiul tău AI</p><h1 className="mt-2 text-3xl font-extrabold text-slate-950">AI Hub</h1><p className="mt-2 text-slate-600">Generează CV optimizat ATS și publică portofoliul.</p></div><div className="grid gap-6 md:grid-cols-2"><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="flex items-center gap-2 text-lg font-extrabold"><Sparkles className="h-5 w-5 text-[#026a81]" />Generează CV</h2><p className="mt-4 text-sm leading-6 text-slate-600">AI-ul folosește exclusiv informațiile completate în profilul tău și nu inventează experiențe sau rezultate.</p>{atsScore !== null ? <p className="mt-4 text-sm font-bold text-[#0e5e6f]">Ultimul scor ATS: {atsScore}%</p> : null}<button onClick={() => setIntent("cv")} disabled={generating} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-lg bg-[#026a81] px-4 py-3 font-bold text-white hover:bg-[#003747] disabled:opacity-70">{generating ? <><Loader2 className="h-4 w-4 animate-spin" />Se generează CV-ul...</> : <><Sparkles className="h-4 w-4" />Generează CV cu AI</>}</button></section><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="flex items-center gap-2 text-lg font-extrabold"><ExternalLink className="h-5 w-5 text-[#026a81]" />Portofoliu public</h2><p className="mt-4 text-sm leading-6 text-slate-600">Portofoliul tău folosește un template EduLink sigur și își actualizează datele direct din profil.</p><button onClick={() => setIntent("portfolio")} className="mt-6 inline-flex w-full items-center justify-center gap-2 rounded-lg border border-[#0e5e6f] px-4 py-3 font-bold text-[#0e5e6f] hover:bg-[#e5f4f6]"><ExternalLink className="h-4 w-4" />Generează portofoliu</button></section></div></main>{intent ? <div role="dialog" aria-modal="true" className="fixed inset-0 z-[70] grid place-items-center bg-slate-950/45 p-4"><section className="w-full max-w-md rounded-2xl bg-white p-6 shadow-2xl"><div className="flex items-start justify-between gap-4"><div><h2 className="text-xl font-extrabold text-slate-950">Înainte de generare</h2><p className="mt-3 text-sm leading-6 text-slate-600">Asigură-te că ai tot profilul completat înainte de a crea cu AI. Nu vor fi procesate informațiile care lipsesc.</p></div><button type="button" onClick={() => setIntent(null)} className="rounded-lg p-1 text-slate-500 hover:bg-slate-100" aria-label="Închide"><X /></button></div><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setIntent(null)} className="rounded-lg border border-slate-300 px-4 py-2 text-sm font-bold text-slate-700">Anulează</button><button type="button" onClick={continueIntent} className="rounded-lg bg-[#026a81] px-4 py-2 text-sm font-bold text-white hover:bg-[#003747]">Înainte</button></div></section></div> : null}</div>;
}
