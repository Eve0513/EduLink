"use client";

import Image from "next/image";
import Link from "next/link";
import { BriefcaseBusiness, CheckCircle2, ExternalLink, FileText, GraduationCap, MapPin, Pencil, Plus, Save, Sparkles } from "lucide-react";
import { useState, useTransition } from "react";
import { toast } from "sonner";
import { saveStudentProfileBasics } from "@/app/actions/onboarding";
import { StudentHeader } from "@/components/dashboard/student-header";

type StudentProfile = { full_name: string; email: string; headline: string | null; location: string | null; bio: string | null; avatar_url: string | null; qr_code_slug: string | null };
type Education = { id: string; institution_name: string; degree: string; field_of_study: string; start_date: string; end_date: string | null; is_current: boolean | null };
type Experience = { id: string; company_name: string; position_title: string; location: string | null; start_date: string; end_date: string | null; is_current: boolean | null; description: string | null };
type Skill = { id: string; name: string; level: string };

export function StudentProfileClient({ profile, educations, experiences, skills, desiredJobTitles }: { profile: StudentProfile; educations: Education[]; experiences: Experience[]; skills: Skill[]; desiredJobTitles: string[] }) {
  const [draft, setDraft] = useState({ fullName: profile.full_name, headline: profile.headline ?? "", location: profile.location ?? "", bio: profile.bio ?? "" });
  const [editing, setEditing] = useState(false);
  const [pending, startTransition] = useTransition();
  const initials = profile.full_name.split(" ").map((word) => word[0]).join("").slice(0, 2).toUpperCase();
  const portfolioUrl = profile.qr_code_slug ? `/portofoliu/${profile.qr_code_slug}` : null;

  function save() {
    startTransition(async () => {
      const result = await saveStudentProfileBasics(draft);
      if (result.error) { toast.error(result.error); return; }
      setEditing(false);
      toast.success("Modificările profilului au fost salvate.");
    });
  }

  return <main className="min-h-screen overflow-hidden bg-[#f5f8f9]"><StudentHeader name={profile.full_name} avatarUrl={profile.avatar_url} current="profile" /><div className="pointer-events-none fixed -left-32 top-44 h-96 w-96 rounded-full bg-[#0e5e6f]/10 blur-3xl" /><div className="pointer-events-none fixed -right-24 top-80 h-80 w-80 rounded-full bg-[#168a9b]/10 blur-3xl" /><div className="relative mx-auto grid max-w-7xl gap-6 px-4 py-7 lg:grid-cols-[minmax(0,1fr)_320px] lg:px-6"><section className="space-y-5"><article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="h-28 bg-[linear-gradient(115deg,#003747,#0e5e6f_55%,#168a9b)]" /><div className="px-5 pb-6 sm:px-7"><div className="-mt-14 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div className="flex items-end gap-4">{profile.avatar_url ? <Image src={profile.avatar_url} alt="Fotografia de profil" width={112} height={112} className="h-28 w-28 rounded-full border-4 border-white object-cover shadow-md" /> : <div className="grid h-28 w-28 place-items-center rounded-full border-4 border-white bg-[#e5f4f6] text-3xl font-extrabold text-[#0e5e6f] shadow-md">{initials}</div>}<div className="pb-1"><h1 className="text-2xl font-extrabold text-slate-950">{draft.fullName || "Profil EduLink"}</h1><p className="mt-1 font-medium text-slate-600">{draft.headline || "Student EduLink"}</p></div></div><button onClick={() => setEditing((value) => !value)} className="inline-flex items-center justify-center gap-2 rounded-lg border border-[#0e5e6f] px-4 py-2 text-sm font-bold text-[#0e5e6f] transition hover:bg-[#e5f4f6]"><Pencil className="h-4 w-4" />{editing ? "Închide editarea" : "Editează profilul"}</button></div><div className="mt-5 flex flex-wrap gap-x-5 gap-y-2 text-sm text-slate-600"><span className="inline-flex items-center gap-1.5"><MapPin className="h-4 w-4 text-[#168a9b]" />{draft.location || "Adaugă locația"}</span><span className="inline-flex items-center gap-1.5"><BriefcaseBusiness className="h-4 w-4 text-[#168a9b]" />{desiredJobTitles.length ? desiredJobTitles.join(" · ") : "Adaugă preferințe job"}</span></div></div></article>{editing ? <section className="rounded-2xl border border-[#0e5e6f]/20 bg-white p-5 shadow-sm sm:p-7"><div className="mb-5 flex items-center gap-2"><Pencil className="h-5 w-5 text-[#0e5e6f]" /><h2 className="text-lg font-extrabold text-slate-900">Editează informațiile de bază</h2></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Nume complet *"><input value={draft.fullName} onChange={(event) => setDraft({ ...draft, fullName: event.target.value })} className={inputClass} /></Field><Field label="Titlu dorit / preferință job"><input value={draft.headline} onChange={(event) => setDraft({ ...draft, headline: event.target.value })} placeholder="Ex.: Frontend Developer Intern" className={inputClass} /></Field></div><div className="mt-4"><Field label="Locație"><input value={draft.location} onChange={(event) => setDraft({ ...draft, location: event.target.value })} placeholder="Ex.: Chișinău, Moldova" className={inputClass} /></Field></div><div className="mt-4"><Field label="Despre mine"><textarea value={draft.bio} onChange={(event) => setDraft({ ...draft, bio: event.target.value })} placeholder="Spune pe scurt ce te interesează și ce îți dorești să înveți." maxLength={500} className={`${inputClass} min-h-32 resize-y`} /><p className="mt-1 text-right text-xs text-slate-400">{draft.bio.length}/500</p></Field></div><div className="mt-5 flex justify-end"><button disabled={pending} onClick={save} className="inline-flex items-center gap-2 rounded-lg bg-[#026a81] px-5 py-3 text-sm font-bold text-white transition hover:bg-[#003747] disabled:cursor-not-allowed disabled:opacity-70">{pending ? "Se salvează..." : <><Save className="h-4 w-4" />Salvează modificările</>}</button></div></section> : null}<ProfileSection title="Despre mine" icon={<FileText className="h-5 w-5 text-[#0e5e6f]" />}><p className="whitespace-pre-wrap leading-7 text-slate-700">{draft.bio || "Adaugă o scurtă descriere pentru ca instituțiile și companiile să te cunoască mai bine."}</p>{!draft.bio ? <button onClick={() => setEditing(true)} className="mt-4 inline-flex items-center gap-2 rounded-lg bg-slate-100 px-4 py-2 text-sm font-bold text-[#0e5e6f] hover:bg-[#e5f4f6]"><Plus className="h-4 w-4" />Adaugă o descriere</button> : null}</ProfileSection><ProfileSection title="Educație" icon={<GraduationCap className="h-5 w-5 text-[#0e5e6f]" />}>{educations.length ? <div className="space-y-5">{educations.map((education) => <div key={education.id} className="flex gap-4"><div className="grid h-11 w-11 shrink-0 place-items-center rounded-xl bg-[#e5f4f6] text-[#0e5e6f]"><GraduationCap className="h-5 w-5" /></div><div><h3 className="font-bold text-slate-900">{education.institution_name}</h3><p className="text-sm text-slate-600">{education.degree} · {education.field_of_study}</p><p className="mt-1 text-sm text-slate-500">{formatDate(education.start_date)} – {education.is_current ? "În curs" : formatDate(education.end_date)}</p></div></div>)}</div> : <AddSection label="Adaugă studii" />}</ProfileSection><ProfileSection title="Experiență & internship-uri" icon={<BriefcaseBusiness className="h-5 w-5 text-[#0e5e6f]" />}>{experiences.length ? <div className="space-y-5">{experiences.map((experience) => <div key={experience.id}><h3 className="font-bold text-slate-900">{experience.position_title}</h3><p className="text-sm text-slate-600">{experience.company_name}{experience.location ? ` · ${experience.location}` : ""}</p><p className="mt-1 text-sm text-slate-500">{formatDate(experience.start_date)} – {experience.is_current ? "Prezent" : formatDate(experience.end_date)}</p>{experience.description ? <p className="mt-2 text-sm leading-6 text-slate-700">{experience.description}</p> : null}</div>)}</div> : <AddSection label="Adaugă o experiență sau un internship" />}</ProfileSection><ProfileSection title={`Competențe (${skills.length})`} icon={<CheckCircle2 className="h-5 w-5 text-[#0e5e6f]" />}>{skills.length ? <div className="flex flex-wrap gap-2">{skills.map((skill) => <span key={skill.id} className="rounded-full bg-[#e5f4f6] px-3 py-1.5 text-sm font-semibold text-[#003747]">{skill.name} · {skill.level}</span>)}</div> : <AddSection label="Adaugă competențe" />}</ProfileSection></section><aside className="space-y-5"><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="text-sm font-bold text-slate-900">CV și portofoliu</p><p className="mt-2 text-sm leading-6 text-slate-600">Completează profilul înainte de generare, pentru rezultate mai bune.</p><Link href="/dashboard/student/ai-hub?intent=cv" className="mt-4 flex w-full items-center justify-center gap-2 rounded-lg bg-[#026a81] px-4 py-2.5 text-sm font-bold text-white hover:bg-[#003747]"><Sparkles className="h-4 w-4" />Generează CV</Link><Link href="/dashboard/student/ai-hub?intent=portfolio" className="mt-2 flex w-full items-center justify-center gap-2 rounded-lg border border-[#0e5e6f] px-4 py-2.5 text-sm font-bold text-[#0e5e6f] hover:bg-[#e5f4f6]"><ExternalLink className="h-4 w-4" />Generează portofoliu</Link></section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-bold text-slate-900">Profil public și adresă URL</p><p className="mt-2 break-all text-sm text-slate-500">{portfolioUrl ?? "Linkul public va fi creat la finalizarea profilului."}</p>{portfolioUrl ? <Link href={portfolioUrl} target="_blank" className="mt-4 inline-flex items-center gap-2 text-sm font-bold text-[#026a81] hover:underline"><ExternalLink className="h-4 w-4" />Previzualizează profilul public</Link> : null}</section><section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><p className="font-bold text-slate-900">Urmărește organizații</p><p className="mt-2 text-sm leading-6 text-slate-600">Găsește instituții și companii relevante în feed, apoi apasă „Urmărește”.</p><Link href="/feed" className="mt-4 inline-flex text-sm font-bold text-[#026a81] hover:underline">Mergi la recomandări</Link></section></aside></div></main>;
}

const inputClass = "mt-1.5 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-900 outline-none transition focus:border-[#026a81] focus:ring-4 focus:ring-[#026a81]/10";
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-sm font-bold text-slate-800">{label}{children}</label>; }
function ProfileSection({ title, icon, children }: { title: string; icon: React.ReactNode; children: React.ReactNode }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:p-7"><h2 className="mb-5 flex items-center gap-2 text-lg font-extrabold text-slate-900">{icon}{title}</h2>{children}</section>; }
function AddSection({ label }: { label: string }) { return <button type="button" onClick={() => toast.info("Secțiunea va fi disponibilă pentru editare în următorul pas.")} className="flex w-full items-center justify-center gap-2 rounded-xl border border-dashed border-slate-300 bg-slate-50 px-4 py-4 text-sm font-bold text-slate-600 transition hover:border-[#0e5e6f] hover:bg-[#e5f4f6] hover:text-[#0e5e6f]"><Plus className="h-4 w-4" />{label}</button>; }
function formatDate(value: string | null) { if (!value) return "—"; const [year, month, day] = value.split("-"); return year && month && day ? `${day}/${month}/${year}` : value; }
